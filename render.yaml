services:
  - type: web
    name: cosmotox-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn agent.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_PATH
        value: database
